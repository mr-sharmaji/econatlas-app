<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/dashboard_home_widget_bg"
    android:orientation="vertical"
    android:padding="14dp">

    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:minHeight="40dp"
        android:orientation="horizontal"
        android:paddingBottom="8dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingEnd="8dp">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/widget_text_primary"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/widget_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/widget_text_secondary"
                android:textSize="10sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/dashboard_home_widget_tab_bg"
            android:gravity="center"
            android:minWidth="56dp"
            android:minHeight="28dp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:text="Refresh"
            android:textColor="@color/widget_blue"
            android:textSize="11sp"
            android:textStyle="bold" />

        <!-- Indeterminate spinner shown while a refresh is in flight.
             onReceive(ACTION_REFRESH_START) hides widget_refresh and
             reveals this. -->
        <ProgressBar
            android:id="@+id/widget_refresh_spinner"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:indeterminate="true"
            android:indeterminateTint="@color/widget_blue"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Tab bar: Markets / Stocks / Mutual Funds. Each tab is a
         TextView pending-intent that switches the active widget
         tab via a broadcast into the provider. Only one is
         highlighted at a time; the Kotlin side drives
         setTextColor + setBackgroundResource for the selected
         state. -->
    <LinearLayout
        android:id="@+id/widget_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="6dp"
        android:weightSum="3">

        <TextView
            android:id="@+id/widget_tab_markets"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/dashboard_home_widget_tab_bg"
            android:gravity="center"
            android:paddingVertical="6dp"
            android:text="Markets"
            android:textColor="@color/widget_text_secondary"
            android:textSize="11sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_tab_stocks"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:background="@drawable/dashboard_home_widget_tab_bg"
            android:gravity="center"
            android:paddingVertical="6dp"
            android:text="Stocks"
            android:textColor="@color/widget_text_secondary"
            android:textSize="11sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_tab_mfs"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:background="@drawable/dashboard_home_widget_tab_bg"
            android:gravity="center"
            android:paddingVertical="6dp"
            android:text="Funds"
            android:textColor="@color/widget_text_secondary"
            android:textSize="11sp"
            android:textStyle="bold" />
    </LinearLayout>

    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:fadingEdge="none"
        android:fastScrollEnabled="false"
        android:layoutAnimation="@anim/widget_tab_layout_anim"
        android:paddingTop="2dp"
        android:scrollbarStyle="outsideInset"
        android:transcriptMode="disabled" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingVertical="20dp"
        android:textColor="@color/widget_text_secondary"
        android:textSize="12sp"
        android:visibility="gone" />
</LinearLayout>
